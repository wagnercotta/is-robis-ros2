# Robis ROS 2

The ROS 2 package is in `odrive_ros2_pkg`.

Launch the complete robot with an optional namespace:

```bash
ros2 launch odrive_ros2_pkg is_robis_ros2_launch.py namespace:=robis
```

See `odrive_ros2_pkg/README.md` for the available launch commands and topic
examples.
